services:
  - type: web
    name: telegram-vip-livepix-pro
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: node src/index.js
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000